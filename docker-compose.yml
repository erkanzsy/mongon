version: "3.8"
services:
  mongon-mongo:
    container_name: mongon-mongo-01
    image: mongo
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example